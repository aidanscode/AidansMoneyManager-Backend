# Aidan's Money Manager - BACKEND

This is the backend-specific repo for Aidan's Money manager

## Notes

* Use following to generate JWT secret: `crypto.randomBytes(20).toString('hex')`
    * Another npm script